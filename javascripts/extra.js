document$.subscribe(function() {
  $('img[usemap]').rwdImageMaps();
});
// This code initializes the rwdImageMaps plugin on all images with a usemap attribute
// when the document is ready, allowing responsive image maps to function correctly.    